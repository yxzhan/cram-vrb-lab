__pycache__/
*.pyc
.venv/
# Generated ROS2 workspace: third-party clones + colcon build output, rebuilt by
# cognitive_robot_abstract_machine/.github/docker/setup_ros_workspace.sh (see README)
ros2_ws/
.ipynb_checkpoints/
iai_apartment/
# Dropped by Isaac's URDF importer when it imports the GARMI description: it
# copies the textures body.mtl/head.mtl reference into `materials/textures`
# relative to the working directory, and records them relative too, so this
# has to stay where it lands. Regenerated on every import.
/materials/
