"""Offline tools over what :mod:`cram_vrb_lab.sim.episode_recording` wrote.

Nothing here runs while the sim does, and nothing here imports Isaac. This is the
half of the data path that happens afterwards, in the CRAM venv, which is the
interpreter that has ``lerobot`` in it -- see
:mod:`cram_vrb_lab.datasets.lerobot_export`.
"""
